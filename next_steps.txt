Phase 5 (API layer) is now in place and wired to the Application layer.

Next is Phase 6: hardening + cross-cutting concerns:
	•	Authentication integration
	•	Implement ICurrentUserAccessor in API (read from claims)
	•	Configure JWT / cookie auth properly
	•	Validation layer
	•	Add FluentValidation for commands
	•	Hook into pipeline (or manual invocation)
	•	Error handling standardisation
	•	Replace BadRequest mapping with proper status codes:
	•	401 / 403 / 404 / 400
	•	Pagination + filtering
	•	Apply to:
	•	GetApplicationsForJob
	•	GetMyApplications
	•	Consistency cleanup
	•	Ensure all endpoints:
	•	use HandleAsync
	•	have consistent route naming
	•	Optional (recommended)
	•	Add request logging / correlation IDs
	•	Add basic API versioning

After that → Phase 7:
	•	file uploads (CVs, attachments)
	•	external integrations
	•	performance + indexing + caching



Short next step: add application create/list endpoints using the new multi-resume model so candidates can choose any active resume when applying, and update employer-side application reads to surface the selected resume cleanly.

which means:
Yes. That is the next logical step after introducing multiple candidate resumes.

What it means:
	•	candidates should be able to apply using any active resume, not just an implicit/default one
	•	application detail/list reads should expose the selected resume metadata clearly
	•	API endpoints should support that choice cleanly

Immediate code work from here is:
	•	update application DTOs to include selected resume info
	•	update GetApplicationByIdHandler
	•	update employer-side application list/detail projections
	•	ensure SubmitJobApplication validates the selected resume belongs to the current candidate
	•	add or refine API endpoints around application submission/read flow if needed

Proceeding with that next is the right move.


Next step is to add actual file upload/storage endpoints and services so resumes are uploaded through the API instead of only being linked by existing StoredFile ids.
so it explained it like this:
Right now resumes must already exist as StoredFile records before they can be used.

Next step means:
	•	Add an endpoint like POST /files
	•	Accept actual file uploads (IFormFile)
	•	Save file metadata to StoredFile
	•	Store file contents (local disk / blob storage)
	•	Return StoredFileId

Then:
	•	Candidate uploads resume → gets StoredFileId
	•	Uses that ID when creating resumes or applying to jobs


i asked it:
summarise this:
Short next step: add candidate resume management endpoints on the minimal API side so uploaded files can immediately be attached as named resumes and then selected during job application.

it said:
Add endpoints for candidates to create/manage resumes (using uploaded StoredFileIds), so they can pick a specific resume when applying to jobs.

it then said:
Next step is to add application submission defaults so if no ResumeFileId is supplied, the candidate’s default active resume is automatically used.
but i didn't ask any other questions, so i asked for the code
